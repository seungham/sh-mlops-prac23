name: GitHub Actions-Manually trigger an Azure Machine Learning job

on:
  [pull_request]

jobs:
  feature-based-dev-setup:
    runs-on: ubuntu-latest
    steps:
    - name: simple execution command
      run: echo "Will add code checks here in next challenge"
